# Global Codex Instructions

These are the user's personal, machine-wide defaults stored under the Codex home
directory. Codex loads them at the lowest precedence, before any repository or
nested AGENTS.md file. This body is padded so the overview section survives
fragment folding and becomes a single `level:global` fact when includeGlobal is
requested by the operator.
