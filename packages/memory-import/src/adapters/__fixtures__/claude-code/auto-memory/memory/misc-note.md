---
name: misc-note
description: A scratch note that carries no metadata block
---

# Scratch note

A miscellaneous note with frontmatter but no `metadata.type`, so it should only
receive the base `claude-code` tag and no second type tag.
