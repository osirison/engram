# Multi Section Note

This preamble overview text is deliberately longer than the two hundred character
minimum-section threshold so that it survives the fragment fold and becomes its
own standalone overview chunk when the vault is imported with splitHeadings on.
Extra padding sentence one. Extra padding sentence two.

## Setup

The setup section content is likewise longer than the two hundred character fold
threshold. It describes installing the dependencies and configuring the local
environment in enough narrative detail that it is never folded into a neighbour.
Extra padding sentence one. Extra padding sentence two here as well.

## Usage

The usage section content is also comfortably over the two hundred character fold
threshold. It explains how to run the tool, which flags exist, and walks through
a couple of representative examples so that it stands on its own as a chunk.
Extra padding sentence one. Extra padding sentence two once more.
