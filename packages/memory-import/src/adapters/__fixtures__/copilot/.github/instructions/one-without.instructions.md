Prefer small, focused commits and conventional commit messages. This scoped
instruction file has no YAML frontmatter at all, so the adapter must tolerate a
missing block and still produce exactly one atomic memory without crashing.
